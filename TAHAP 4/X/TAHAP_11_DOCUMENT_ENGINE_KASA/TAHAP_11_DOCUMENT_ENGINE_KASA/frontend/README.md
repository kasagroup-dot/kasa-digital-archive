# KASA Digital Archive — Frontend Tahap 11

React/Vite + FilePond Document Engine.

Setelah replace file, jalankan `npm install` karena Tahap 11 menambah `filepond` dan `react-filepond`, lalu `npm run dev`.
